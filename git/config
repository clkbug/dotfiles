[user]
	email = mail@clkbug.net
	name = clkbug
[core]
	editor = vim
    # pager = difft
    # pager = delta

[alias]
    graph = log --graph --date=short --decorate=short --pretty=format:'%Cgreen%h %Creset%cd %C(cyan)%cn %Cred%d %Creset%s'

[init]
	defaultBranch = main

[diff]
        tool = difftastic
    external = difft

[difftool]
        prompt = false

[difftool "difftastic"]
        cmd = difft "$LOCAL" "$REMOTE"

[pager]
        difftool = true


# [delta]
#     navigate = true    # use n and N to move between diff sections
#     side-by-side = true
#     # delta detects terminal colors automatically; set one of these to disable auto-detection
#     # dark = true
#     # light = true
# 
# [merge]
#     conflictstyle = diff3
# 
# [diff]
#     colorMoved = default
